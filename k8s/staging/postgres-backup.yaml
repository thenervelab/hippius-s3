apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: postgres
spec:
  backup:
    barmanObjectStore:
      destinationPath: "s3://BACKUP_BUCKET_PLACEHOLDER/staging/postgres/"
      endpointURL: "BACKUP_ENDPOINT_PLACEHOLDER"
