apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: hippius-s3-otel
  labels:
    release: rancher-monitoring
spec:
  selector:
    matchLabels:
      app: otel-collector
  endpoints:
    - port: prometheus
      interval: 30s
      path: /metrics
