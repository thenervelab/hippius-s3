apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: hippius-s3-cache
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: gateway
  endpoints:
    - port: http
      interval: 15s
      path: /metrics
