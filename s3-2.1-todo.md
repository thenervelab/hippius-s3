# s3-2.1 — drain hardening & production readiness

**Status (2026-07-20).** The Rust drain stack runs on staging as the sole upload producer [*arch]. The correctness /
hardening / observability work is **done and merged** (#235/#236/#237, plus #241 reaper-index/alert hardening, #284
metrics fix, #286 read-wait re-poll, #285 prod provisioning manifests, #288/#289 audit waves, perf waves #266–#283)
[*shipped] — **the real bugs already shipped.** What remains splits cleanly into: a **short critical path to cut over**
(deploy mechanics + a focused safety gate), and a larger **exhaustive-validation + optional-hardening program that can
run as monitored fast-follow** — justified because the cutover is a reversible image revert and the janitor never evicts
an un-replicated chunk. Everything below is verified against the tree.

Companion docs: `path-to-prod.md` (the cutover runbook — hybrid rollout, placeholders, env, rollback) ·
`s3-prod-drain-capacity-plan.md` (node sizing) · `stress-test/plan.md` (how to run the harness).

## Why now — this cutover removes a production SPOF [*spof]
On **2026-07-18** `node6-cache` froze (OVH: hardware) and **all** of prod S3 returned 5xx across **both** ATS
edge regions, because the **entire prod write path is pinned to that one node**: `api` + `arion-uploader` +
`arion-downloader` + `backup` + `hydrator` + `janitor` are bound to `k8s-v3-node6-cache` by
`local-cache-patch.yaml` (the 40 TB `local-cache-pvc` is a node-local PV). Random host failure is
unavoidable; a **total platform outage** from one node is not. The staging drain-direct architecture is the
fix (one ingest pod per node + Ceph read-fallback → lose a node, degrade not black out). So this promotion is
**not just "new arch"** — it is **removing the node6 monolith**. Full evaluation + incident RCA:
`2026-07-18-s3-resilient-cutover-remove-monolith.md`.

---

# TO SHIP (critical path)

### Deploy steps (with the rollout) [*deployorder] — full ordered runbook now in `path-to-prod.md`
- [ ] **Run the Python migrations** (incl. `object_versions.completed_part_numbers` + the two reaper indexes). Workers init-gate on this. [*initgates]
- [ ] **Confirm the cephor schema is present** — the drain-allocator owns it; the reaper init-gates on it.
- [ ] **Set `redis-queues` to `noeviction` in prod and restart it**; confirm `CONFIG GET maxmemory-policy`. [*noeviction]
- [ ] **Give every worker its full env/secret set** — config validation is whole-config, even unused keys.
- [ ] **Confirm the redis-queues memory alert thresholds** (warn ~70% / page ~85% of 2 GB). ✅ DLQ + drain alert *rules* now in `alert-rules.yml` (#289); just confirm thresholds post-deploy. [*monfix]
- [x] ~~**Add the MPU-reaper prod-scale indexes.**~~ **DONE** — both migrations landed (`20260702000000_multipart_uploads_initiated_at`, `20260706000000_parts_upload_uploaded_at`, #241); they apply with the standard migration run. Still: raise `HIPPIUS_MPU_REAPER_INTERVAL_SECONDS` off the 120 s drain-down value + `EXPLAIN` at prod scale (R2 gate). [*reaper]

### Cutover mechanics — code promotion, CI, and monolith removal [*cutover]
These are the deploy-plumbing steps the manifests alone don't cover (from the 2026-07-18 resilience plan;
`path-to-prod.md` is the ordered runbook). Prod deploys from the **`k8s-production`** branch, not `staging`.
- [ ] **Reconcile the source of truth** — promote `staging` → `k8s-production`, **carrying forward the prod-only commits** (currently **5**, not the 2 the plan first counted): #258 (delete/unpin + auth/cache/observability), #260 (flag-gated batch-delete unpinner), and the three redis-queues `noeviction`/4 GB-cap hotfixes (#287 + 2). **Do NOT** cherry-pick #265 (metrics fix — superseded by staging's otel rework). [*promote]
- [x] ~~**Add the `build-drain` CI job to `production-deploy.yaml`**~~ **AUTHORED** (branch `feat/prod-drain-ci-and-monolith-removal`, pending merge+promotion) — ported `rust-gate` + `build-drain` from staging (no chaos-mesh/toxiproxy), `deploy needs: [build-images, build-drain]`, drain image tag added. [*ci]
- [x] ~~**Fix the prod rollout-waits**~~ **AUTHORED** (same branch) — rewritten shape-robust: waits on a Deployment only if present (`api` → `api`+`api-local` → `api-local`), plus allocator/reaper waits + the drain-agent DaemonSet ≥1-Ready logic. Pre-cutover deploys still pass. [*ci]
- [x] ~~**Un-pin the node6 workers + delete the monolith manifests**~~ **AUTHORED** (same branch) — dropped `local-cache-patch`/`configmap-patch` + `pv`/`pvc-local-cache` from the overlay and deleted the 4 files; `api` + `arion-uploader`/`downloader`/`janitor` revert to un-pinned CephFS `object-cache` (verified render: 0 `local-cache-*`, 0 `k8s-v3-node6-cache`). `backup`/`hydrator` are external (not in this repo). **Cutover-safety:** drain the old upload queue before the promotion abandons the node6 `local-cache-pvc`. [*monolith]
- [ ] **Reclaim node pod-slots by cleanup, NOT a `maxPods` raise** — node4/node5 sit near the 110-pod cap on terminal junk (Completed/Error/CrashLoop pods, TTL-less Jobs) while CPU/RAM are idle. Reap terminal pods + add `ttlSecondsAfterFinished` to the offending Jobs → frees far more than the ~2 slots/node the ingest tier needs. [*podcap]
- [ ] **Single-node-kill drill (acceptance test)** — after cutover, cordon+drain (or stop) ONE ingest node: S3 stays serving, 5xx at baseline, drain resumes on recovery. This is the definition of done for the SPOF fix. [*killdrill]

### Ship gate (run on staging — the safety-critical subset) [*gate]
Nothing is trusted unless `inv-guard` stayed green for the whole run. [*invdet]
- [ ] **Ratify the data-safety SLOs with Ops** against a real in-cluster baseline (laptop numbers invalid). [*slo]
- [ ] **Durability green** — S8 byte-identical re-GET (client-side md5 / `x-amz-checksum`, not ETag). *Non-overridable.*
- [ ] **Single-leader / no split-brain** — S10/S11, including the allocator-failover race (F2 + T4 Scenario C / R3). *Non-overridable.* [*t4c]
- [ ] **R2 prod-scale query gate** — index scans only, no seq scans / spills. *Missing index = NO-GO.* [*r2]
- [ ] **Focused chaos** — F4 (CephFS slow/down) + F5 (SSD fill) within their recovery bounds, zero invariant break. [*chaos]

### Runbooks + alerts (before cutover)
- [x] ~~**Rollout/rollback runbook**~~ **DONE** — `path-to-prod.md` (drain stack ships first; zero-downtime dual-image hybrid; rollback = scale/route revert). [*rollback]
- [ ] **Alerting rehearsed** — ✅ rules exist (`alert-rules.yml`: DLQ depth/growth/dropped, split-brain, epoch-decrease, SSD-pressure, aged-orphan, corrupt, reclaim-errors, #241/#289); still rehearse the fire→page→ack path once on prod.

### Decisions that unblock the above [*decisions]
- [ ] **SLO ratification** (owner) — highest priority; gates the most.
- [ ] **`O_NOFOLLOW` trust boundary** — is the SSD cache trusted (our pods only)? If yes, the `O_NOFOLLOW` fix drops off entirely.
- [ ] **Chaos-tier gating** — confirm the focused subset above is enough to ship, with the full matrix as fast-follow.
- [x] ~~**Prod topology**~~ **DECIDED + PREPPED** (#285, #292) — 5 ingest nodes (`k8s-v3-node1..5`), each `api-local` (replicas=5) + `drain-agent` DaemonSet + singleton allocator. **The nodes already have a dedicated 3.84 TB Gen4 NVMe mounted at `/s3-data`** (swept 2026-07-21 — no Ceph-OSD repurpose needed after all), and are **labeled `s3-prod-local-ingest=true`** on the live cluster; the manifests point at `/s3-data` with the real node list (#292). Manifests still commented out in `kustomization.yaml` pending the CI/code-promotion work below. Sizing: `s3-prod-drain-capacity-plan.md`.

---

# FAST-FOLLOW (post-cutover, monitored — not release-blocking)

### Code hardening (latent / optional)
- [ ] **Tier-1 landing fast-path** — the top residual latency item. #286 shipped the read-wait FS re-poll (a fresh cross-node read no longer *hangs* — it picks up the drained part within ~1 poll), but the part is still invisible until the reconciler records it, so a read racing a fresh write waits 0–`CEPHOR_RECONCILE_POLL_SECS` (15 s). An api landing signal + mtime-incremental scan collapses this to ~1 s. See `drainer-optimisations.md` Tier 1.
- [ ] **C1c agent-side epoch check** — the write-side split-brain fence already landed; this is the read-side belt-and-suspenders. [*c1c]
- [ ] **`O_NOFOLLOW` on drain file opens** — only if the trust-boundary decision says defend against planted symlinks. [*nofollow]
- [ ] **A16 S4-append-after-replication** — verify under soak; fix only if it actually leaks. [*a16]
- [ ] **A20 middleware pressure hysteresis** — janitor side done; middleware still flaps at the threshold (cosmetic). [*a20]
- [ ] **R5 stalled-drain alert** — "throughput == 0 AND backlog > 0", independent of the lag histogram. [*r5]
- [ ] **Dead-code cleanup** — remove `enqueue_upload` (0 callers) + the stale root `CLAUDE.md` §3.1 step-9 line.

### Full validation program (the rest of WI-19)
- [ ] **Load driver + profiles** — certify the per-node knee; scaling/soak/backpressure ladders. [*load]
- [ ] **Rust §4.8 test tiers** — `loom` (greenfield), the missing proptests, and the real-PG/Redis fault-injection suite. [*rust48]
- [ ] **Full T4 allocator rigs A/B/D/E/F** — beyond the failover race in the ship gate. [*t4c]
- [ ] **Full chaos matrix** — F1, F3, F6, F7, F8 (the ship gate runs only F2/F4/F5). [*chaos]
- [ ] **6h soak** — RSS/FD/pool/redis slopes ≈ 0; `replicated`-on-SSD + aged-pending-orphan counts bounded. [*soak]
- [ ] **Offline history checker** — `inv/history_check.py` (Elle/Knossos over the recorded op log).
- [ ] **inv-guard event-driven upgrade** — poller today; WAL/log-tail so a between-polls violation isn't missed. [*invguard]
- [ ] **Test-coverage gaps** — targeted tests for A7 part-subset, A8 chunk_backend revive, A14 KEK singleflight, B4/B5, the janitor hysteresis band, A20.

### Later (separate track)
- [ ] **Edge auto-caching** — cache read traffic at the edge incl. private content, to bridge the cross-node SSD→pool window. EC-1 auth probe → EC-2 passive → EC-3 default → EC-4 config mgmt. [*edge]

---

## Notes (technical context to keep the list actionable)

[*arch] **Architecture.** Node-local SSD ingest → CephFS pool via the Rust hippius-drain stack (`drain-allocator`
singleton + `drain-agent` DaemonSet). The API writes each part to SSD + `object_versions.address` and **never enqueues
uploads**; the **drain is the sole producer** — once a part replicates it LPUSHes an `UploadChainRequest`, the uploader
ships it to the backend and records `chunk_backend`. State machine `cephor_replication_status`:
`pending → draining → replicated | failed | corrupt`. The janitor evicts a CephFS part only once every required backend
has a live `chunk_backend` row (or the part is terminally abandoned). Cross-node reads in the SSD→pool window use
`DualFileSystemPartsStore` pool fallback. Cutover is flagless (no producer toggle; rollback = image revert). Prod
Postgres is on dedicated NVMe; prod adds dedicated SSD ingest nodes — a full SSD 503s every PUT on that node.

[*shipped] **What shipped (do not redo).** #218 (WI-1…WI-17 hardening) · #220–#225 (worker metrics, observability,
reaper safety, staging reconcile) · #227 (drain throughput throttle — AIMD `TARGET_P99_MS` 50 ms → 2000 ms, the S1
unblocker) · #228 (A7 ETag/order reject) · #230 (A1 cold-fallback DCR) · #231/#232 (lag tune, poll 5 s → 1 s) · #233
durability + #234 coordination, both rolled into **#235** · #236 (read-path A2–A5) · #237 (A6 ACL fallback, A10 DLQ cap,
A12 downloader retry). #235 also added: R4 first-class `corrupt` state + bounded re-drive, C1b/C1c write-side epoch
fence, C2/WI-20 SSD reclaim + orphan-GC, C7/C8/C10/C11/C13, A8/A9/A11/A13/A14/A15/A17/A18/A19/A21, and the WI-19 fault
substrate (mock faults, toxiproxy toxics + redis/PG proxies, noeviction override, Rig A/B, Chaos-Mesh F1–F8 CRDs,
inv-det/inv-guard, durability ledger). **#226 is superseded by #235** (fully contained).

[*deployorder] **Deploy order.** The drain owns the Rust `cephor_*` schema; the Python migrator owns `schema_migrations`.
Apply: `db-migrations` Job → API/gateway → workers → mpu-reaper. initContainers self-sequence (workers wait on
`wait-for-migrations`; the reaper also waits on `wait-for-cephor-schema`). Then run `tests/smoke` (the staging e2e).

[*initgates] **A18/A19 (done, #235).** mpu-reaper `wait-for-cephor-schema` initContainer
(`to_regclass('cephor_replication_status')`); the four DB workers + reaper have `wait-for-migrations`
(`scripts/wait_for_migrations.py` polls `schema_migrations`). New migration: `object_versions.completed_part_numbers`
(`20260706130000`, nullable → backward-compatible).

[*noeviction] **redis-queues noeviction.** `noeviction` is inert until the process restarts. It holds the work queues
+ pub/sub + `cephor:*` lease/fence keys, so a full instance must reject writes loudly, never evict. Staging e2e is
`allkeys-lru`; the prod-parity override for tests is `stress-test/compose/docker-compose.faults.yml`. AOF reload blips
`cephor:*` — sequence the restart in a window.

[*monfix] **Monitor fix (done, #235 Group 5).** `metrics_collector_task.py` now reads `used_memory`/`maxmemory` from
`redis_queues_client` (was the main redis). Gate on the *instance*, not the `:6382` port (dev-only label; in k8s
redis-queues is a separate host). Just confirm the alert thresholds are set post-deploy.

[*reaper] **Reaper indexes (R2).** Before enabling on prod: (1) run the `20260702000000`
`multipart_uploads(initiated_at)` partial index (not on prod yet); (2) ~~add a composite
`parts(upload_id, uploaded_at)` index~~ **built (`20260706000000`, `CREATE INDEX CONCURRENTLY`) —
#241** (a failed CONCURRENTLY build still needs a manual `REINDEX`/drop — a dbmate `transaction:false`
migration must stay a single statement, so it can't self-heal); (3) raise `HIPPIUS_MPU_REAPER_INTERVAL_SECONDS` off the 120 s
drain-down value. The reaper query also reads `object_versions.address` — the whole drain stack is
staging-only today.

[*gate] **Gate structure.** T1 functional/adversarial · T2 invariant continuous-assertion (`inv-det` + `inv-guard`,
the backbone) · T3 chaos · T4 allocator-under-load · T5 throughput/scale/soak. Every dynamic run runs on top of T2. The
ship gate above is the safety-critical subset (durability, split-brain, R2, F2/F4/F5); the rest is fast-follow.

[*slo] **SLOs (DRAFT — ratify with Ops).** S1 per-node throughput ≥ 200 MB/s at knee (sources disagree 50–200; set CI
floor ~70% of measured p50). S2/S3 replication-lag p99 < 15 s below knee / < 60 s at knee. S4 SSD backlog < 20 GiB/node.
S5/S6 503 < 1% at knee, non-503 5xx < 0.1%. S7 recovery < 120 s. **S8 durability 100% byte-identical** (client-side
plaintext md5 / `x-amz-checksum-*`, NOT ETag) *(non-overridable)*. S9 DLQ growth 0 nominal. **S10/S11 exactly 1 leader,
0 unforced churn** *(non-overridable)*. S12 replication gate 0 unreplicated deletions even ≥95% disk. S13 0 redis-queues
evictions under `noeviction`. S14 6h soak slopes ≈ 0. Lag = SQL on `cephor_replication_status (updated_at − landed_at)`;
there is no `drain_replication_lag_seconds` metric. Only S8 + S10/S11 are in the ship gate; S1–S7/S9/S13/S14 are
fast-follow knee/soak certification.

[*invdet] **Running the invariant harness.** `inv-det` (deterministic pre-deploy): `cargo test -p hippius-drain-core`
(**`--include-ignored` + real Redis**), `pytest tests/unit tests/integration`, targeted e2e, static callsite audit,
`trybuild`. **Local drain Rust tests are easy to run wrong** — a bare `cargo test -p hippius-drain-core` silently skips
most: DB tests (`#[sqlx::test]`) need `DATABASE_URL` → Postgres on **:5433** (`postgres:postgres@localhost:5433`); the
system Postgres on :5432 has no `postgres` role (confusing `role "postgres" does not exist`). Coord/tick tests are
`#[ignore]` + need `CEPHOR_TEST_REDIS_URL=redis://localhost:6382/0` and `-- --include-ignored`; `coordination`/`tick`
are behind `--features coord`, `store` behind `--features pg`. Full gate:
`DATABASE_URL=postgres://postgres:postgres@localhost:5433/postgres CEPHOR_TEST_REDIS_URL=redis://localhost:6382/0 cargo test -p hippius-drain-core --features coord -- --include-ignored`.
`inv-guard` guards: G1 single-leader+epoch, G2 replication-gate, G3 durability re-GET, G4 sole-producer, G6 terminal
monotonicity, G9 aged-pending-orphan backlog (G5/G7/G8 covered by inv-det/scenario, reported as `skip`).

[*t4c] **T4 Scenario C = the R3 failover race.** `run_tick` captures `epoch` at lease-acquire, then does slow
`ceiling()` → `write_allocations` with no lease re-read, so a deposed leader (epoch N) that finishes before the
successor (N+1) writes any alloc key lands an epoch-N budget the fence lets through. **Fixed on #235** (fold the epoch
into the Lua CAS conditioned on current `cephor:epoch`) with a coordination-unit regression + a `run_tick` failpoint
test, both fail pre-fix. Scenario C must still exercise it under *live* failover: stale budget visible ≤ 1 alloc-TTL
(15 s) AND ≤ 1 agent poll (2 s), else FAIL. Note the C1c read-side residual (fast-follow). Rigs are built
(`crates/hippius-drain-allocator/tests/alloc_stress.rs` + `stress-test/alloc-stress/`); only the run remains.

[*r2] **R2 prod-scale gate.** Reaper/reconciler `SELECT`s on prod-sized tables (`multipart_uploads` ~92M / `parts`
~94M) are ~30 s cold / ~478 ms warm, ~3 GB buffers. Gate on **buffers** (no `Seq Scan`, no Hash `Batches>1`) with
`EXPLAIN(ANALYZE,BUFFERS)` at prod scale + HypoPG/Dexter; rewrite predicates as `col = ANY($1::uuid[])`, not
`scalar = ANY(array_col)`. `scripts/gen_clean_dump.py` is absent (doc bug in `CLAUDE.md`).

[*chaos] **Chaos F1–F8.** Ship gate runs F2/F4/F5; the rest is fast-follow. F1 agent-kill mid-replication (resume
≤ 60 s). **F2 allocator failover/fence** — 3 sub-injections: clean SIGTERM / partition-heal / 2-replica race
(single-leader ≤ 45 s). F3 redis-queues blip/evict/hang/noeviction-full (coord ≤ 30 s). **F4 CephFS slow/down** (breaker
≤ 20 s, no false `replicated`). **F5 SSD fill** (clean 503, replication gate absolute). F6 postgres failover (breaker
must NOT trip on PG-only fault). F7 clock skew (disposable pods only). F8 corrupt/partial chunk (never commit corrupt;
exercises R4). CRDs in `k8s/chaos/`, executor in `stress-test/faults/`. F3 needs the noeviction override; F4 IOChaos on
CephFS is risky (prefer NetworkChaos toward OSD/MDS).

[*rollback] **Rollback.** Write path is drain-direct + flagless → rollback = revert images to the prior tag. Migrations
are additive (`completed_part_numbers` nullable = "all parts") so a version-N-1 API tolerates the new column. A stuck
drain is not data loss: acked bytes are on ingest SSD and the janitor's replication gate never evicts an un-replicated
chunk.

[*decisions] **Also open (fast-follow/team):** central Prometheus reachability vs port-forward for inv-guard; F4 CephFS
realism (single-pod IOChaos vs shared-pool NetworkChaos); 3rd staging ingest node for the ≥80% scaling curve; LB routing
across the 2 api-local pods; F7 `clockIds` (monotonic vs wall); WI-18 supervisor fault policy (fail-fast recommended);
AIMD smoothing (deferred until T5 load data); C1 epoch durability across AOF reload/failover (consider a Postgres-backed
epoch anchor); post-commit enqueue durability (may `replicated` diverge from actually-uploaded? add a re-derive sweep or
accept).

[*c1c] **C1c residual.** `drain-agent/src/runtime.rs` `pull_action` (~line 359) does
`Ok(Some(alloc)) => Adopt(alloc.budget)` with no `StoredAllocation.epoch` comparison. The write-side Lua CAS fence on
`cephor:epoch` is correct; the read-side agent should reject a stale-epoch alloc key. Pairs with its §4.8 fault-injection
test (`C1c epoch-bypass`).

[*nofollow] **O_NOFOLLOW.** No symlink refusal anywhere in the agent/core localfs open path. Filesystem hardening;
prerequisite for the §4.8 symlink-refused test. Drops off entirely if the trust boundary is "our pods only".

[*a16] **A16.** Post-replication S4 append reaching a backend relies on the drain re-scan (drain-direct). The
"terminally-drained → SSD-only → lost on reclaim" edge is *mitigated* (not closed) by the R4 servability gate. File:
`append.py`.

[*a20] **A20.** `fs_pressure.py` middleware still uses a single free-space threshold (it got Retry-After ±25% jitter,
not an enter/exit band). The janitor already has hysteresis (elevated 0.85/0.83, critical 0.95/0.93).

[*r5] **R5.** Alerts that exist: `drain-split-brain-two-leaders`, `drain-leader-epoch-decrease`,
`aged-pending-orphan-backlog`, SSD backlog/pressure. **#241** hardened these: `drain_leader_epoch` now resets on lease
loss so the epoch-decrease alert can actually fire, guarded (`and max(...) > 0` + `for: 2m`) so a normal
leadership-handoff vacuum can't false-page; + a reclaim-disabled alert and a corrupt-parts standing gauge; and the
wedge/readiness signal now keys on an **undrained-row COUNT** (not a byte SUM a missing `parts` row could zero) so a
wedged node reads NotReady. **Still missing:** the dedicated stall alert on `throughput==0 ∧ backlog>0` derived from
SQL/Redis, independent of the lag histogram (a stalled drain emits no lag samples). F4 / agent-kill exercises it.

[*load] **Load driver.** Have: boto3 fixed corpus, hardcoded rungs (5/10/20), the durability ledger. Missing:
A1/A2/GB/soak/backpressure/scaling ladders, hold-SLO-for-duration ramp → certified knee, in-cluster GB driver. Fork:
boto3-native vs warp/elbencho.

[*rust48] **Rust §4.8 tiers.** `loom` tier greenfield (no `loom` dep). Missing proptests: AIMD
multiplicative-decrease/saturation (incl. `error_bps` + `!Open`), `NearFull`-ceiling conservation, 50..1000-node fleet,
epoch `>` vs `≥`. Missing fault-injection (real PG + Redis): concurrent `claim_part` N=2..16, N-way lease, multi-key
all-or-nothing, C1c epoch-bypass, Redis DEBUG SLEEP/black-hole, noeviction-OOM typed Err, fsync EIO, `O_NOFOLLOW`,
two-node same-part, C13 through real faults.

[*soak] **Soak/knee.** Certify the per-node knee (S1) and hold each SLO for the ramp duration. 6h soak:
RSS/FD/pool/redis slopes ≈ 0 (≤ +2%/h) AND `replicated`-still-on-SSD count bounded (C2/R1) AND aged-pending orphan
count bounded (A21 — the replicated-only gate is blind to it). **#241** makes the aged-pending-orphan **gauge** usable
for this gate: decoupled onto a short 1 h visibility grace (`aged_orphan_gauge_grace_seconds`) so a soak-fresh leak
registers within the run, and DLQ-protected versions excluded so the gauge counts the same population the sweep clears.

[*invguard] **inv-guard upgrade.** Today it polls with ≥4× lookbehind + hysteresis; hard binary invariants want
event-driven log/DB tailing so a fast violation between polls isn't missed. Fork: trigger+LISTEN/NOTIFY vs
logical-decode slot vs log-tail + keep polling.

[*edge] **Edge auto-caching.** EC-1 must authorize *every* request including cache hits and demote visibility to
`private, no-store` on egress; verify on staging before EC-2/EC-3. Future: edge-validated signed tokens/URLs if per-hit
auth costs too much.

[*spof] **The SPOF (why this is now urgent).** Prod's cache is a **node-local** 40 TB ZFS PV on `node6-cache`
(`local-cache-pvc`), and `local-cache-patch.yaml` pins the whole write path there. The 2026-07-18 freeze proved a single
node = total S3 outage. The target arch spreads ingest across nodes with a CephFS read-fallback, so a node loss degrades
(the other ingest pod + pool fallback keep serving) instead of blacking out. Incident evidence: abrupt unlogged host
freeze (no OOM/MCE/panic in the journal), recovered only by an OVH power-cycle — install `rasdaemon` on the workers so
the next event is diagnosable, and chase OVH's IPMI SEL / intervention report.

[*cutover] **Cutover style — hybrid vs full-swap.** Two ways to flip routing, both reversible:
(a) **zero-downtime dual-image hybrid** (`path-to-prod.md`): run the OLD image (self-enqueue + node6 cache) and the NEW
`api-local` (drain-direct + SSD) behind the same `api` Service via a shared pod label, shift traffic by replica count,
scale old→0 once proven. Safe because both producers LPUSH the **identical `UploadChainRequest`** to the same
`{backend}_upload_requests` (golden-fixture-tested in `crates/hippius-drain-agent/src/enqueue.rs`). **The one hard rule:**
a NEW-image api must never write the ceph cache (it doesn't self-enqueue and the drain only scans node-local SSD → stuck
`pending`); so the ceph writer stays the OLD image, the SSD writer is `api-local`, and the drain deploys BEFORE any
`api-local` takes traffic. During the hybrid, node6 is only a *partial* path (a node6 loss now degrades, not blacks out)
and the SPOF is fully gone once base `api`→0. (b) **full-swap** (staging end-state; the 2026-07-18 plan's Phase 5):
Service→`app: api-local` + base `api`→0 in one step. Prefer (a) for the first prod cutover.

[*promote] **Code promotion.** `main..staging` ≈ 280 commits. `staging..main` = only #265 (superseded — do not
cherry-pick). The prod-only commits that MUST carry forward live on `k8s-production`: #258, #260, and the redis-queues
`noeviction`/4 GB-cap hotfixes (#287 + 2). Pick a git strategy (make `main` trunk then fast-forward `k8s-production`, or
merge `staging`→`k8s-production` directly) — both are the same ~280-commit promotion.

[*ci] **CI gaps in `production-deploy.yaml`.** No `build-drain` job (staging's is rust-gated, builds
`ghcr.io/thenervelab/hippius-s3/drain`) → port it, excluding chaos-mesh/toxiproxy. Rollout-status waits on
`deployment/api` → must wait on `api-local` and add allocator/agent waits. Prod app image must be **≥ #265** for
trustworthy OTel metrics.

[*monolith] **#285 added, it did not remove.** #285 authored `k8s/production/{ingest-node-labels,api-local,drain-agent,
drain-allocator,mpu-reaper}.yaml` (commented out in `kustomization.yaml`) — the plan's "author prod overlay" (Phase
3.1–3.3) is effectively done. Still open: the CI/rollout changes [*ci], and the kustomization flip that **un-pins the
workers and deletes** `local-cache-patch.yaml` / `pv-local-cache.yaml` / `pvc-local-cache.yaml` /
`local-cache-configmap-patch.yaml`.

[*podcap] **Pod-slot crunch is cleanup, not limits.** node4/node5 are near the 110-pod cap on terminal pods
(Completed/Error/CrashLoop) + TTL-less indexer/marketing Jobs while CPU 3–10 % / RAM 25–56 %. Reap terminal pods + add
`ttlSecondsAfterFinished` → frees far more than the 2 slots/node the ingest tier needs. Owner call on the long-lived
crash-loopers (e.g. Sentry ×9 for ~80 d) and whether to **taint** the ingest nodes so staging/dev can't recompete.

[*killdrill] **Definition of done.** Losing any single node no longer outages S3 (the kill-drill passes); `k8s/production`
has no node6 pin and no `local-cache-*`; node6's freed 40 TB NVMe becomes **ingest node #1** (cheaper than pulling a Ceph
OSD for the first node); the SPOF is retired. Ceph is GO (75 TiB free) but note the standing `HEALTH_WARN` (mons low on
mon-store disk — fix separately, not a cutover blocker) and the added pool read-load from moving cache reads onto Ceph.

---

# PROD LOGISTICS — provisioning the SSD-ingest tier on the existing cluster

Full analysis + formulas + references live in **`s3-prod-drain-capacity-plan.md`**. Manifests are in
**`k8s/production/`** (created here, **commented out** in `kustomization.yaml` until nodes are prepped).

### Sizing (measured from prod `object_versions` + Loki, NOT the metrics — those are inflated pre-#265)
- Real ingest: **avg 32 MB/s, peak-5min 470 MB/s, peak-1min 573 MB/s, ~2.5 TB/day (peak day 5.2 TB)**;
  objects avg 21/s, peak 150/s; **bimodal** — 91% of objects <64 KB, 92% of bytes are 1–10 MB.
- Verdict: **light on bytes, heavy on count, very bursty (~18×).** Binding constraint = **drain
  throughput**; SSD is sized for **endurance/headroom**, not capacity (resident backlog ~10–25 GB).
- **Recommendation: 3–4 ingest nodes**, each `api-local` + `drain-agent`, with **1–2 TB
  write-intensive NVMe (≥3 DWPD)**. Drainers are a DaemonSet on those nodes — no separate hosts.

### Nodes are already provisioned — dedicated `/s3-data` NVMe (no OSD repurpose) [DONE 2026-07-21, #292]
Swept all five: **`k8s-v3-node1..5` each have a dedicated 3.84 TB Gen4 datacenter NVMe mounted (ext4) at
`/s3-data`** (WD Ultrastar DC on node1/3/5, Micron 7450 PRO on node2, Intel/Solidigm D7-P5520 on node4;
~empty, all `rota=0`). Each drive does ~2.5–3.4 GB/s write vs the ~115 MB/s/node demand (~20× headroom);
endurance need ≈0.13 DWPD, well under the drives' ~1 DWPD — so these read-intensive parts are fine here.
The earlier "repurpose a Ceph OSD" plan is **moot** — the disks are already there and mounted.

- **Labels applied on the live cluster:** `kubectl label node k8s-v3-node{1..5} s3-prod-local-ingest=true`
  (node2/node3 also keep `s3-staging-local-ingest` — independent selectors; staging writes the node-root
  `local_ingest_staging` path, prod writes `/s3-data`, so no disk collision).
- **Manifests updated (#292):** node allow-lists → node1..5, `hostPath /s3-data` (`type: Directory` latch:
  pod won't start if `/s3-data` isn't mounted), `api-local` replicas=5.
- **Still NOT done:** nodes are not prod-*exclusive*. Making them so needs a TAINT + tolerations, which
  would evict the general worker pool — a separate capacity call ([*podcap]). node4/node5 are near the
  110-pod cap, so the +2 pods/node (api-local + drain-agent) may want the pod cleanup first.

### To actually deploy (remaining, in `path-to-prod.md`)
1. Land the CI + code-promotion + monolith-removal work ([*ci] / [*promote] / [*monolith]) — the manifests
   can't ship without the `build-drain` job, and the prod image must be **≥ #265** (metrics fix, PR #284).
2. Uncomment the drain block **and** the `drain` image in `k8s/production/kustomization.yaml`.
3. Standard deploy order ([*deployorder]): migrations → allocator (owns `cephor_*` schema, first) →
   api-local + agent + reaper.
4. **Cutover routing** — the zero-downtime hybrid or the full-swap ([*cutover]); rollback = re-point the
   Service + scale base api back up (acked bytes on SSD are never lost — the janitor's replication gate).

### Prod deltas already baked into `k8s/production/*` (vs the staging copies)
`s3-prod-local-ingest` label · **`/s3-data` hostPath** · `postgres-nvme-rw` (prod's NVMe PG) ·
`ENVIRONMENT=production` · `hostPath type: Directory` · node1..5 allow-lists · the tuned drain knobs (AIMD
floor 50 MB/s / target 8 s, `DRAIN_CONCURRENCY=16`, `RECONCILE_POLL=15`, `DRAIN_POLL=1`). With 5 nodes the
default `CEPHOR_MAX_DRAIN_RATE_BPS` (100 MB/s/node) is already ~870 MB/s fleet — ample over the ~573 MB/s
peak; no raise needed.
