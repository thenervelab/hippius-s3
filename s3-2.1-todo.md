# s3-2.1 — drain hardening & production readiness

> Living TODO for the Rust `hippius-drain` stack (lives on the `staging` branch). One surgical PR per work item.

## Architecture (current)

Node-local SSD ingest → CephFS pool via the Rust hippius-drain stack (`drain-allocator` singleton + `drain-agent`
DaemonSet). The API writes each part to node-local SSD + `object_versions.address`; it **never enqueues uploads**.
The **drain is the sole upload producer**: once a part replicates to the pool it LPUSHes an `UploadChainRequest` to
Redis; the uploader ships it to the storage backend and records `chunk_backend`. State machine:
`cephor_replication_status` = `pending → draining → replicated | failed` (terminal sinks). The janitor evicts a
CephFS part only once every required backend has a live `chunk_backend` row — or once the part is terminally
abandoned.

The cutover is **flagless**: the drain is the sole upload producer with no fallback, so every availability item
below is live today, not a latent prod-only risk.

**Priority note:** prod Postgres runs on dedicated NVMe; prod adds dedicated SSD nodes for ingest. So the PG-relief
track is churn-reduction (not a cliff), and SSD cleanup is required (a full SSD 503s every PUT on that node).

## Done

- **Drain-direct cutover** (#199): drain enqueues uploads; API writes `address`; deferral/backoff; MPU reaper for
  abandoned/aborted uploads; dual-FS read-fallback; cross-language wire-contract golden test.
- **Goal 1 — SSD-ingest reclaim worker** (#201): GCs `failed`-only SSD parts + orphan write-temps past a grace.
- **Goal 2 — throughput + Redis coordination** (#212): batch fsync; sampled readback; tunable drain concurrency;
  allocator lease/heartbeats/allocations moved to a Redis `Coordinator` (epoch fence via atomic Lua CAS).
- **CephFS-pool reclaim of abandoned parts** (#214): closes the last permanent leak — the janitor reaps a part only
  when `failed` **and** the version is unservable (`address IS NULL` + zero size/md5).
- **Append double-produce fix** (#217): append routed through `set_object_version_address` like PUT/MPU, restoring
  the sole-producer invariant.
- **Production-readiness hardening** (#218, *in review — CI green*): lands every P1+P2 audit finding (WI-1…WI-17):
  data-integrity (completeness check + full per-chunk verify + bounded retry + claim-fenced `mark_failed`); drain
  OTLP metrics → Grafana; `redis-queues` → `noeviction`; CI runs the coordination test tier; liveness probes +
  non-zero exit on fault; breaker `Probing` deadline + non-Ceph carve-out; Redis command timeouts + decay-on-error;
  batched reconciler; terminal-row GC; ancestor-dir fsync; backends-from-secret + schema-wait initContainer.
  Reviewed 2026-07-01 — no P1/P2 code blockers; the actionable items are the two deploy prerequisites below.

## Work items

### Deploy prerequisites for #218 (before/with the staging rollout)
- **Restart `redis-queues`** — the `noeviction` policy is inert until the process restarts; a normal `apply` rolls
  the pod, but confirm `CONFIG GET maxmemory-policy` == `noeviction` post-deploy and sequence it in a window (the
  restart briefly blips the `cephor:*` lease/fence keys during AOF reload). The change lives in `k8s/base`, so it
  reaches prod on next apply even though the drain is staging-only.
- **Wire a `redis-queues` memory alert** — with `noeviction`, a full instance rejects writes *loudly* (drain can't
  enqueue), which is safe only if it's visible before it fills. Ship the alert alongside #218, not "later".

### Remaining (post-#218)
- **WI-18 — resilience + hygiene polish.** Cheap/safe follow-ups: stuck-defer visibility metric, worker readiness
  probes, `O_NOFOLLOW` on SSD opens, allocator non-root `securityContext`, full-jitter backoff. **Defer** AIMD
  smoothing (EWMA/dead-band/safe-floor) until WI-19 gives real load data. **Skip** (low value): the
  arithmetic/indexing lint pass (churn) and `write_allocations` lease revalidation (marginal now redis is
  `noeviction`).
- **WI-19 — scale + chaos validation** (Phase-4 prod gate). Two halves: an e2e chaos-correctness suite (`tests/e2e`;
  kill node, Redis blip/eviction/hang, Ceph degradation) with an **end-to-end checksum no-data-loss gate**, and a
  GB-scale throughput/lag benchmark (`hippius-benchmarks`). Gated on the SLO numbers below.
- **Observability (Python side, complements #218's WI-4).** Instrument the un-metered loops (MPU reaper first;
  orphan-checker, account-cacher, cachet, DLQ), then build the "S3 Drain" Grafana dashboard consuming the new
  `drain_*` metrics + reaper/janitor counters. Plus: real-PG reaper integration test + `multipart_uploads.initiated_at`
  index; a co-located throughput benchmark.

### Decisions needed (owner / team)
- **SLOs for WI-19:** target drain throughput per node (MB/s); max replication lag (p99 `updated_at − landed_at`);
  backlog alert threshold (undrained SSD bytes); chaos environment (staging vs a dedicated test namespace).
- **WI-18 design calls:** supervisor fault policy — fail-fast (worker dies → pod restarts; *recommended*) vs
  per-worker restart + backoff; AIMD smoothing — recommend deferring until WI-19 load data.

## Edge auto-caching

Goal: auto-cache read traffic at the edge — **including private content** — to (a) bridge the cross-node window
between a fresh upload landing on one node's SSD and its replication to the shared pool (a GET routed to a different
node currently 503s until replication), and (b) hold a transient secondary copy during that window. The edge cache
is best-effort/evictable — never a durability guarantee; the read path must always fall back SSD → pool → backend.

Current state: the gateway side is built (cache-control policy, an auth-probe endpoint, and cache purge-on-write);
the edge configuration that uses them is not yet deployed, and content is private-by-default.

- **EC-1 — private-content auth probe.** Wire the edge auth-subrequest to the gateway's existing probe endpoint,
  plus a visibility demotion (store cacheable at the edge, but rewrite to `private, no-store` on egress so clients
  don't cache it). Security linchpin: authorize **every** request, including cache hits — otherwise cached private
  content is served unauthorized. Deploy + verify on staging first.
- **EC-2 — passive auto-cache.** With EC-1 live, any successful GET (public or private) populates the edge, so
  subsequent cross-node reads during the replication window are served from cache. (Active post-upload warming is a
  later option, only if residual first-read 503s prove material.)
- **EC-3 — cache-by-default flip (gated).** Only after EC-1 is proven: make new buckets cacheable by default
  (explicit + revertable), so the edge caches everything under the auth-probe gate.
- **EC-4 — edge config management.** Bring the edge fleet under config management and reconcile drift, so staging
  mirrors prod and changes apply uniformly.
- **Future:** if the per-request auth round-trip proves costly, migrate to edge-validated signed tokens/URLs (the
  CDN-standard model — presigned URLs already provide a token-like primitive).

## Path to production

Sequenced; each phase gates the next. Nothing ships to prod until the topology sign-off.

1. **Correctness + hardening + observability (staging).** Landed in **#218** (data integrity, decay-on-`Err`,
   metrics, breaker/Redis-timeout, reconciler/GC, `noeviction`, liveness, exit-on-fault). *Remaining:* merge #218,
   run its deploy prerequisites (redis restart + memory alert), instrument the Python loops + build the dashboard.
   *Gate:* on the staging dashboard, `drain_*` series are visible, `drain_leader=1`, parts keep reaching
   `replicated`, and no probe flapping / init CrashLoop.
2. **Scale & chaos validation.** WI-19, after the SLOs are set. *Gate:* throughput acceptable, lag bounded, every
   failure self-recovers, and byte-for-byte integrity confirmed by checksum.
3. **Edge auto-caching (staging).** EC-1 (auth-probe + visibility demotion) and EC-4 (edge config management), then
   EC-2/EC-3. *Gate:* no private-data leak on a cache hit is proven; cross-node gap bridging works; per-hit auth
   latency acceptable.
4. **Prod readiness.** Rollout/rollback runbooks (drain ships before the API image that stops PUT-enqueue);
   alerting on drain lag, SSD pressure, DLQ growth, coordinator leader-loss/fence anomalies. *Gate:* runbooks +
   alerts rehearsed on staging.
5. **Topology decision meeting.** SSD ingest nodes (count/sizing vs lag + pressure), taints/isolation, scaling
   strategy (DaemonSet + singleton placement, graceful node add/drain). (F3/epoch-eviction is already solved by
   `noeviction`; a dedicated coordination Redis is now only optional defense-in-depth.) *Gate:* signed-off topology
   + cutover runbook.
